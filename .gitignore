# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

# editor / IDE
.idea/
.vscode/
.claude/

# backups and scratch
*.bak
temp_*.jsx
temp_*.js
test_chord_format.js
test_chords_complex.js

# Raw asset drops / one-off dumps — kept locally, never committed (Han 2026-08-31, "commit alles")
# 622MB profiler trace + 381MB third-party SFZ sample pack: source material, not repo material.
src/assets/ASSET DROP/Trace-*.json
src/assets/ASSET DROP/MuldjordKit SFZ+WAV-*/

# FluidR3_GM.sf2 (141MB) — build-time-only source for scripts/extract-soundfont-samples.mjs.
# The EXTRACTED output (src/audio/localInstrumentBuffers.generated.js) is what the app loads and
# IS committed. Drop this .sf2 in locally only if you need to re-run the extraction. Stripped from
# history 2026-09-01 (exceeded GitHub's 100MB limit) — see scripts/extract-soundfont-samples.mjs.
src/assets/FluidR3_GM.sf2
